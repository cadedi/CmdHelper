<Window x:Class="LinuxCmdHelper.DataSourceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LinuxCmdHelper"
        mc:Ignorable="d"
        Title="数据源管理与远程接口同步" Height="580" Width="780"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#EEEEEE" Foreground="#111827"
        FontFamily="Segoe UI, Microsoft YaHei, Arial">

    <Window.Resources>
        <Style x:Key="DialogBtnPrimary" TargetType="Button">
            <Setter Property="Background" Value="#0284C7"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogBtnSecondary" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 头部说明 -->
            <RowDefinition Height="*"/>
            <!-- 数据源列表 -->
            <RowDefinition Height="Auto"/>
            <!-- 添加/编辑表单 -->
            <RowDefinition Height="Auto"/>
            <!-- 底部操作栏 -->
        </Grid.RowDefinitions>

        <!-- 头部说明 -->
        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="📡 多数据源 &amp; 远程 API 接口同步" FontSize="16" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,4"/>
                <TextBlock Text="支持配置多个远程 HTTP/HTTPS 接口或本地 JSON 文件。可选用【智能去重合并 (按 ID 覆盖旧项)】或【完全替代】，支持离线断网自动缓存。" FontSize="12" Foreground="#4B5563" LineHeight="18" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- 数据源列表 -->
        <Border Grid.Row="1" Background="#FFFFFF" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="4,0,4,8">
                    <TextBlock Text="已配置的数据源列表" FontSize="13" FontWeight="Bold" Foreground="#374151" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtSourceCount" Text="共 0 个数据源" FontSize="12" Foreground="#6B7280" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </Grid>

                <ListBox x:Name="LstDataSources" Grid.Row="1" BorderThickness="0" Background="Transparent" SelectionChanged="LstDataSources_SelectionChanged">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,0,0,6"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Padding="12,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <CheckBox Grid.Column="0" IsChecked="{Binding Enabled}" VerticalAlignment="Center" Margin="0,0,10,0" Checked="ChkSourceEnabled_Changed" Unchecked="ChkSourceEnabled_Changed" ToolTip="开启/停用此数据源"/>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="13" Foreground="#111827" Margin="0,0,8,0"/>
                                            <Border Background="#E0F2FE" CornerRadius="4" Padding="6,1" Margin="0,0,6,0">
                                                <TextBlock Text="{Binding MergeMode}" FontSize="10" Foreground="#0369A1" FontWeight="SemiBold"/>
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding Url}" FontSize="11" FontFamily="Cascadia Code, Consolas" Foreground="#6B7280" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding LastStatus}" FontSize="11" Foreground="#059669" Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <Button Grid.Column="2" Content="删除" Tag="{Binding Id}" Style="{StaticResource DialogBtnSecondary}" Click="BtnDeleteSource_Click" VerticalAlignment="Center" Padding="8,4"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- 添加/编辑表单 -->
        <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="添加新的数据源 / 接口" FontSize="13" FontWeight="Bold" Foreground="#374151" Margin="0,0,0,10"/>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="120"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="TxtNewName" Grid.Column="0" Style="{StaticResource InputBoxStyle}" Text="公司内网接口" Margin="0,0,8,0" ToolTip="数据源名称"/>
                    <TextBox x:Name="TxtNewUrl" Grid.Column="1" Style="{StaticResource InputBoxStyle}" Text="https://api.example.com/ops/commands.json" Margin="0,0,8,0" ToolTip="支持 HTTP/HTTPS 接口 URL 或 本地 .json 文件绝对路径"/>
                    <ComboBox x:Name="CmbMergeMode" Grid.Column="2" Height="36" Margin="0,0,8,0" VerticalContentAlignment="Center" SelectedIndex="0">
                        <ComboBoxItem Content="智能去重合并 (merge)" Tag="merge"/>
                        <ComboBoxItem Content="完全覆盖替代 (replace)" Tag="replace"/>
                    </ComboBox>
                    <Button x:Name="BtnAddSource" Grid.Column="3" Content="➕ 添加数据源" Style="{StaticResource DialogBtnPrimary}" Click="BtnAddSource_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- 底部操作栏 -->
        <Grid Grid.Row="3">
            <TextBlock x:Name="TxtSyncStatus" Text="" FontSize="12" Foreground="#0284C7" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnSyncNow" Content="🔄 立即测试并同步全部数据源" Style="{StaticResource DialogBtnPrimary}" Click="BtnSyncNow_Click" Margin="0,0,10,0"/>
                <Button x:Name="BtnClose" Content="完成并关闭" Style="{StaticResource DialogBtnSecondary}" Click="BtnClose_Click" Width="90"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
